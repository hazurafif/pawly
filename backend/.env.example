# Pawly server configuration — copy to .env and adjust.
# Real environment variables override these; CLI flags override everything.

# TCP port to listen on (default 8080).
PAWLY_PORT=8080

# Directory for the SQLite database and photo binaries (default ./data).
# Relative paths resolve against this .env file's directory.
PAWLY_DATA_DIR=./data
